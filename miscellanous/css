/* ==========================================================================
   1. CSS RESET & GLOBAL BOX-SIZING
   ========================================================================== */
*,
*::before,
*::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

html {
    font-size: 100%; /* 1rem = 16px */
    scroll-behavior: smooth;
    -webkit-text-size-adjust: 100%;
}

body {
    background-color: var(--color-bg-light);
    color: var(--color-text-dark);
    font-family: var(--font-primary);
    font-size: var(--fs-body);
    line-height: 1.65;
    letter-spacing: 0.015em;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
}

/* ==========================================================================
   2. CUSTOM CSS VARIABLES (DESIGN TOKENS)
   ========================================================================== */
:root {
    /* Color Palette */
    --color-primary: #1b4332;         /* Deep Forest Green */
    --color-primary-hover: #081c15;
    --color-secondary: #2d6a4f;       /* Emerald Accent */
    --color-accent: #d97706;          /* Warm Gold/Amber */
    --color-accent-hover: #b45309;
    --color-text-dark: #1f2937;       /* Dark Charcoal Body Text */
    --color-text-light: #f9fafb;      /* Clean White */
    --color-bg-light: #f3f4f6;        /* Light Gray Background */
    --color-bg-card: #ffffff;
    --color-border: #d1d5db;
    --color-shadow-sm: 0 2px 6px rgba(0, 0, 0, 0.06);
    --color-shadow-md: 0 8px 18px rgba(0, 0, 0, 0.10);
    --color-shadow-lg: 0 16px 32px rgba(0, 0, 0, 0.15);

    /* Typography Scale (Rem Units) */
    --font-primary: 'Segoe UI', system-ui, -apple-system, BlinkMacSystemFont, Roboto, sans-serif;
    --fs-h1: 2.25rem;    /* 36px */
    --fs-h2: 1.75rem;    /* 28px */
    --fs-h3: 1.35rem;    /* 21.6px */
    --fs-body: 1rem;      /* 16px */
    --fs-small: 0.875rem; /* 14px */

    /* Spacing System */
    --spacing-xs: 0.5rem;   /* 8px */
    --spacing-sm: 1rem;     /* 16px */
    --spacing-md: 1.5rem;   /* 24px */
    --spacing-lg: 2.5rem;   /* 40px */
    --spacing-xl: 4rem;     /* 64px */

    /* Radii & Transitions */
    --radius-sm: 6px;
    --radius-md: 12px;
    --transition-standard: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

/* ==========================================================================
   3. BASE TYPOGRAPHY
   ========================================================================== */
h1, h2, h3, h4 {
    color: var(--color-primary);
    font-weight: 700;
    line-height: 1.25;
    letter-spacing: -0.02em;
    margin-bottom: var(--spacing-sm);
}

h1 { font-size: var(--fs-h1); }
h2 { font-size: var(--fs-h2); }
h3 { font-size: var(--fs-h3); }

p {
    margin-bottom: var(--spacing-sm);
}

a {
    color: var(--color-primary);
    text-decoration: none;
    transition: var(--transition-standard);
}

a:hover, 
a:focus {
    color: var(--color-accent);
}

ul, ol {
    list-style-position: inside;
    margin-bottom: var(--spacing-sm);
}

/* ==========================================================================
   4. NAVIGATION & HEADER LAYOUT
   ========================================================================== */
header {
    background-color: var(--color-primary);
    box-shadow: var(--color-shadow-md);
    position: sticky;
    top: 0;
    z-index: 1000;
}

nav {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--spacing-sm);
}

nav ul {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    list-style: none;
    margin: 0;
    padding: 0;
}

nav ul li a {
    color: var(--color-text-light);
    display: inline-block;
    padding: 1.25rem 1rem;
    font-weight: 600;
    font-size: var(--fs-small);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border-bottom: 3px solid transparent;
}

/* Pseudo-Class States for Navigation Links */
nav ul li a:hover,
nav ul li a:focus {
    color: var(--color-accent);
    background-color: rgba(255, 255, 255, 0.08);
}

nav ul li a:active {
    transform: scale(0.97);
}

nav ul li a.active {
    color: var(--color-accent);
    border-bottom-color: var(--color-accent);
}

/* ==========================================================================
   5. MAIN CONTENT & HERO BANNER LAYOUT
   ========================================================================== */
main {
    flex: 1;
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
    padding: var(--spacing-lg) var(--spacing-sm);
}

/* Section Header Banners */
.page-title {
    text-align: center;
    margin-bottom: var(--spacing-lg);
}

.page-title p {
    color: #4b5563;
    font-size: 1.125rem;
    max-width: 720px;
    margin: 0 auto;
}

/* Hero Section Layout (Flexbox / Overlay) */
.hero-banner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background-color: var(--color-bg-card);
    border-radius: var(--radius-md);
    overflow: hidden;
    margin-bottom: var(--spacing-xl);
    box-shadow: var(--color-shadow-md);
    border: 1px solid var(--color-border);
}

.hero-content {
    flex: 1;
    padding: var(--spacing-lg);
}

.hero-content h2 {
    font-size: var(--fs-h1);
    color: var(--color-primary);
    margin-bottom: var(--spacing-sm);
}

.hero-image {
    flex: 1;
    max-height: 420px;
    overflow: hidden;
}

.hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: var(--transition-standard);
}

.hero-banner:hover .hero-image img {
    transform: scale(1.03);
}

/* ==========================================================================
   6. GRID & FLEXBOX COMPONENT STYLES
   ========================================================================== */
/* Responsive Cards Grid */
.cards-grid, 
.team-grid, 
.services-detail {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-xl);
}

/* Card Styling & Hover Dynamics */
.card, 
.team-card, 
.mv-box, 
.contact-card, 
.map-card {
    background-color: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    padding: var(--spacing-md);
    box-shadow: var(--color-shadow-sm);
    transition: var(--transition-standard);
    position: relative;
    overflow: hidden;
}

.card:hover, 
.team-card:hover, 
.contact-card:hover {
    transform: translateY(-6px);
    box-shadow: var(--color-shadow-lg);
    border-color: var(--color-secondary);
}

.card img, 
.team-card img, 
.service-block img {
    width: 100%;
    height: 200px;
    object-fit: cover;
    border-radius: var(--radius-sm);
    margin-bottom: var(--spacing-sm);
}

/* Event Tag Overlay */
.event-tag {
    display: inline-block;
    background-color: var(--color-accent);
    color: #ffffff;
    font-size: var(--fs-small);
    font-weight: 700;
    padding: 0.25rem 0.75rem;
    border-radius: var(--radius-sm);
    margin-bottom: var(--spacing-xs);
}

/* Zebra Striping using :nth-child */
.cards-grid article:nth-child(even) {
    background-color: #fafafa;
}

/* Impact Metrics Grid */
.impact-section {
    background-color: var(--color-primary);
    color: var(--color-text-light);
    padding: var(--spacing-lg) var(--spacing-md);
    border-radius: var(--radius-md);
    text-align: center;
    margin-bottom: var(--spacing-xl);
}

.impact-section h2 {
    color: var(--color-accent);
    margin-bottom: var(--spacing-md);
}

.stats-container {
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    gap: var(--spacing-md);
}

.stat-box h3 {
    color: var(--color-text-light);
    font-size: 2.25rem;
    margin-bottom: 0.25rem;
}

.stat-box p {
    color: #d1fae5;
    margin-bottom: 0;
}

/* About Us Section Layout */
.history-section {
    display: flex;
    gap: var(--spacing-md);
    align-items: center;
    margin-bottom: var(--spacing-xl);
}

.history-text {
    flex: 1;
}

.history-image {
    flex: 1;
}

.history-image img {
    width: 100%;
    border-radius: var(--radius-md);
    box-shadow: var(--color-shadow-md);
}

.mission-vision {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-xl);
}

.mv-box {
    border-left: 5px solid var(--color-accent);
}

/* Contact Grid */
.contact-details-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-md);
}

/* ==========================================================================
   7. FORM & BUTTON STYLING
   ========================================================================== */
.form-section {
    max-width: 780px;
    margin: 0 auto;
    background: var(--color-bg-card);
    padding: var(--spacing-lg);
    border-radius: var(--radius-md);
    border: 1px solid var(--color-border);
    box-shadow: var(--color-shadow-md);
}

fieldset {
    border: 1px solid var(--color-border);
    padding: var(--spacing-md);
    border-radius: var(--radius-sm);
    margin-bottom: var(--spacing-md);
}

legend {
    color: var(--color-primary);
    font-weight: 700;
    padding: 0 var(--spacing-xs);
}

.form-group {
    margin-bottom: var(--spacing-sm);
}

.form-group label {
    display: block;
    font-weight: 600;
    margin-bottom: 0.35rem;
}

.form-group input,
.form-group select,
.form-group textarea {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    font-family: inherit;
    font-size: var(--fs-body);
    background-color: #f9fafb;
    transition: var(--transition-standard);
}

/* Pseudo-Class States for Form Controls */
.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
    outline: none;
    border-color: var(--color-primary);
    background-color: #ffffff;
    box-shadow: 0 0 0 3px rgba(27, 67, 50, 0.2);
}

/* Buttons */
.btn {
    display: inline-block;
    padding: 0.85rem 1.75rem;
    font-size: 1rem;
    font-weight: 700;
    border: none;
    border-radius: var(--radius-sm);
    cursor: pointer;
    text-align: center;
    transition: var(--transition-standard);
    margin-right: var(--spacing-xs);
    margin-bottom: var(--spacing-xs);
}

.btn-primary {
    background-color: var(--color-accent);
    color: #ffffff;
}

.btn-primary:hover, 
.btn-primary:focus {
    background-color: var(--color-accent-hover);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(217, 119, 6, 0.35);
}

.btn-primary:active {
    transform: translateY(0);
}

.btn-secondary {
    background-color: var(--color-secondary);
    color: #ffffff;
}

.btn-secondary:hover, 
.btn-secondary:focus {
    background-color: var(--color-primary);
    transform: translateY(-2px);
}

/* ==========================================================================
   8. FOOTER STYLING
   ========================================================================== */
.main-footer {
    background-color: var(--color-primary);
    color: var(--color-text-light);
    padding: var(--spacing-lg) 0 var(--spacing-sm) 0;
    margin-top: auto;
    border-top: 4px solid var(--color-accent);
}

.footer-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--spacing-sm);
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: var(--spacing-md);
}

.footer-links ul {
    list-style: none;
}

.footer-links a {
    color: #d1fae5;
}

.footer-links a:hover {
    color: var(--color-accent);
}

.copyright {
    text-align: center;
    margin-top: var(--spacing-md);
    padding-top: var(--spacing-sm);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    font-size: var(--fs-small);
}

/* ==========================================================================
   9. RESPONSIVE MEDIA QUERIES
   ========================================================================== */

/* --------------------------------------------------------------------------
   TABLET BREAKPOINT (max-width: 1023px)
   -------------------------------------------------------------------------- */
@media screen and (max-width: 1023px) {
    :root {
        --fs-h1: 1.85rem;
        --fs-h2: 1.5rem;
    }

    main {
        padding: var(--spacing-md) var(--spacing-sm);
    }

    .hero-banner {
        flex-direction: column;
    }

    .hero-image {
        width: 100%;
        max-height: 280px;
    }

    .history-section {
        flex-direction: column;
    }

    .contact-details-grid {
        grid-template-columns: 1fr;
    }

    .cards-grid, 
    .team-grid {
        grid-template-columns: repeat(2, 1fr); /* 2 Columns on Tablet */
    }
}

/* --------------------------------------------------------------------------
   MOBILE BREAKPOINT (max-width: 767px)
   -------------------------------------------------------------------------- */
@media screen and (max-width: 767px) {
    :root {
        --fs-h1: 1.5rem;
        --fs-h2: 1.25rem;
        --fs-h3: 1.1rem;
        --fs-body: 0.95rem;
    }

    /* Stacked Mobile Navigation Menu */
    nav ul {
        flex-direction: column;
        width: 100%;
        padding: var(--spacing-xs) 0;
    }

    nav ul li {
        width: 100%;
        text-align: center;
    }

    nav ul li a {
        width: 100%;
        padding: 0.75rem var(--spacing-sm);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    nav ul li a.active {
        border-bottom: 1px solid var(--color-accent);
        background-color: rgba(255, 255, 255, 0.12);
    }

    /* Single-Column Stack Layouts */
    .cards-grid, 
    .team-grid, 
    .services-detail, 
    .mission-vision {
        grid-template-columns: 1fr; /* Single column on phones */
    }

    .stats-container {
        flex-direction: column;
        gap: var(--spacing-sm);
    }

    .form-section {
        padding: var(--spacing-sm);
    }

    .btn {
        width: 100%; /* Full width buttons on mobile touch screens */
        margin-right: 0;
    }

    .footer-content {
        flex-direction: column;
        text-align: center;
    }
}
